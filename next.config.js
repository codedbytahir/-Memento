/**
 * Configuration file for the Next.js framework.
 * It defines build-time settings such as React strict mode to ensure application stability and performance.
 */
const nextConfig = {
  reactStrictMode: true,
};

module.exports = nextConfig;
